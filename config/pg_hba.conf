# config/pg_hba.conf
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (inside the container)
local   all             all                                     trust
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

# Replication from docker network (replica container)
host    replication     replicator      172.16.0.0/24           scram-sha-256

# Docker network connections (inter-container)
host    all             all             172.16.0.0/24           scram-sha-256

# Host machine connections (via Docker port mapping)
host    all             all             0.0.0.0/0               scram-sha-256
